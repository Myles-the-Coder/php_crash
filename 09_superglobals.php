<?php
// Super global variables
// Built in variables that at always available in all scopes

// $_GET - Array of variables passed to the current script via the URL parameters
// $_POST - Array of variables passed to the current script via the HTTP POST method
// $_FILES - Array of files sent to the current script
// $_COOKIE - Array of variables passed to the current script via HTTP cookies
// $_SESSION - Array of variables passed to the current script via a session
// $_SERVER - Array of information about the current server
// $_ENV - Array of information about the current environment
// $_REQUEST - Array of HTTP request headers
?>

